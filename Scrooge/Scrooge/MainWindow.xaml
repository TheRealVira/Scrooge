<Window x:Class="Scrooge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Scrooge"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{StaticResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="Scrooge" Height="549.5" Width="799.5">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Edit">
                <MenuItem Header="_Cut" Command="Cut">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ContentCut"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Copy" Command="Copy">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ContentCopy"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="_Paste" Command="Paste">
                    <MenuItem.Icon>
                        <materialDesign:PackIcon Kind="ContentPaste"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar Style="{DynamicResource MaterialDesignToolBar}">
                <Button ToolTip="Lookup Github" Click="GithubButton_Onclick">
                    <materialDesign:PackIcon Kind="GithubBox"/>
                </Button>
            </ToolBar>
            <ToolBar Style="{DynamicResource MaterialDesignToolBar}">
                <Button ToolTip="Stackoverflow" Click="StackoverflowButton_Onclick">
                    <materialDesign:PackIcon Kind="Stackoverflow"/>
                </Button>
            </ToolBar>
        </ToolBarTray>
        <Grid VerticalAlignment="Stretch" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <materialDesign:PackIcon Kind="AlertCircle"/>
            <TextBlock Grid.Column="1"  Margin="8 0 0 0">Test:</TextBlock>
            <TextBox Grid.Column="2" Margin="8 0 0 0" VerticalAlignment="Center"></TextBox>
            <Button Grid.Column="3" Margin="8 0 0 0">Submit</Button>

            <materialDesign:Card Grid.ColumnSpan="4" Margin="32" Grid.Row="1" materialDesign:ShadowAssist.ShadowDepth="Depth3">
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal" Margin="16 0 0 0">
                    <materialDesign:PackIcon Kind="Twitter" Height="64" Width="64"/>
                    <TextBlock VerticalAlignment="Center" TextWrapping="Wrap" Margin="16 16 0 0">Hey, just don't.</TextBlock>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:ColorZone Grid.ColumnSpan="4" Margin="32" Grid.Row="2" Height="100" Mode="PrimaryDark" CornerRadius="8" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                <StackPanel Orientation="Horizontal" Margin="16 16 0 0">
                    <materialDesign:PackIcon Kind="Facebook" Height="64" Width="64" VerticalAlignment="Center"/>
                    <TextBlock VerticalAlignment="Center" TextWrapping="Wrap" Margin="16 16 0 0">DON'T use me ;D.</TextBlock>
                    <materialDesign:ColorZone Mode="Light">
                        <StackPanel Orientation="Horizontal" Margin="16">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="Magnify"/>
                            <TextBox VerticalAlignment="Center" BorderThickness="0" materialDesign:TextFieldAssist.DecorationVisibility="Collapsed" materialDesign:TextFieldAssist.Hint="Please enter something to search!" MinWidth="120" Margin="8 0 0 0"></TextBox>
                            <Button VerticalAlignment="Center" Style="{DynamicResource MaterialDesignToolButton}" Margin="8 0 0 0"> <materialDesign:PackIcon Kind="Microphone"/> </Button>
                        </StackPanel>
                    </materialDesign:ColorZone>
                </StackPanel>
            </materialDesign:ColorZone>
        </Grid>
    </DockPanel>

    <!--<TabControl x:Name="tabControl" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TabItem Header="Balance">
                <StackPanel>
                    <ListView x:Name="listView2">
                        <ListBoxItem Content="Erlöse $ 500"/>
                        <ListBoxItem Content="Ausgaben $ 500"/>
                        <ListBoxItem Content="Einkommen $ 500"/>
                        <ListBoxItem Content="Essen $ 50"/>
                    </ListView>
                    <Button>Add account</Button>
                </StackPanel>
            </TabItem>
            <TabItem Header="Inventory">
                <StackPanel>
                    <ListView x:Name="listView">
                        <ListBoxItem Content="Car"/>
                        <ListBoxItem Content="House"/>
                        <ListBoxItem Content="Lamp"/>
                        <ListBoxItem Content="Can"/>
                    </ListView>
                    <Button>Add account</Button>
                    <Button>Summarize accounts</Button>
                </StackPanel>
            </TabItem>
            <TabItem Header="Report" />
        </TabControl>
        <StatusBar Margin="0,486,670,0" >
            <ListBoxItem Content="Warnings"/>
        </StatusBar>
    </Grid>-->
</Window>
